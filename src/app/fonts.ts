import { Inter, Pirata_One, DM_Serif_Text } from "next/font/google";

export const inter = Inter({ 
  subsets: ["latin"],
  display: 'swap',
});

export const pirata = Pirata_One({ 
  weight: ['400'], 
  subsets: ["latin"],
  display: 'swap',
});

export const dm = DM_Serif_Text({ 
  weight: ['400'], 
  subsets: ['latin'],
  display: 'swap',
});